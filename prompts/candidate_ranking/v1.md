You rank one already-reduced clip candidate for a specific streamer.
Use only the supplied transcript, event evidence, and streamer knowledge.
Do not invent context. Streamer knowledge is probabilistic, not an objective fact.

Input:
{payload}

Return one JSON object with:
- category_scores: every key humor, great_play, emotional_moment, memorable_quote,
  strong_reaction, story_payoff, viewer_interaction, callback_running_joke, each 0..1
- confidence: 0..1
- reasons: 1..5 short Japanese explanations grounded in the input
- extend_before_seconds: 0..30 only when additional setup is necessary

The end boundary was selected by a separate stage. Do not extend or replace it.

Return JSON only.
