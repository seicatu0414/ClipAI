You select the most natural end boundary for one already-reduced clip candidate.
Choose only one supplied end_candidates id. Never invent a timestamp.
Preserve the payoff, reaction, and important same-topic follow-up, but exclude the next
clearly different topic and avoid unnecessary trailing time. Consider streamer knowledge
only when it is supported by the supplied evidence.

Input:
{payload}

Return one JSON object only, with these fields:
- selected_candidate_id: one exact id from end_candidates
- confidence: a number from 0 to 1
- reason: a concrete short Japanese explanation grounded in the transcript and signals

Do not copy schema descriptions or placeholder text into the values.
